name = "policywatch-api"
main = "src/index.ts"
compatibility_date = "2025-08-01"

[[r2_buckets]]
binding = "BUCKET"
# Must be lowercase (Workers rule). Use the exact name from Cloudflare → R2.
bucket_name = "policywatch-data"
